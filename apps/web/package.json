{
  "name": "@acme/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host --port 4321 --strictPort",
    "build": "astro build && node ./scripts/fix-vercel-output.mjs",
    "vercel-build": "pnpm build",
    "preview": "astro preview --host --port 4321",
    "lint": "astro check && tsc --noEmit",
    "typecheck": "astro check && tsc --noEmit"
  },
  "dependencies": {
    "@acme/shared": "workspace:*",
    "@astrojs/vercel": "^8.2.9",
    "@tailwindcss/vite": "^4.1.13",
    "astro": "^5.13.5",
    "semver": "^7.7.3",
    "tailwindcss": "^4.1.13",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
